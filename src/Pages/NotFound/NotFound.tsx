import { Layout } from '../../Components/Layout/Layout';
import './NotFound.css';

type Props = {}
export function NotFound(props: Props): JSX.Element {

    return (
        <Layout>
            <h1>NotFound</h1>
        </Layout>
    )
}